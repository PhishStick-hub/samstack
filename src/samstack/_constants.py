"""Internal constants shared across samstack fixtures."""

# Floci accepts any non-empty value for AWS credentials.
# "test" is its documented default and is not configurable.
FLOCI_ACCESS_KEY = "test"
FLOCI_SECRET_KEY = "test"
